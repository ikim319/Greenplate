<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GreenPlate"
        tools:targetApi="31">

        <activity
            android:name=".views.PersonalInformation"
            android:exported="false" />

        <activity
            android:name=".views.ShoppingList"
            android:exported="false" />
        <activity
            android:name=".views.Ingredients"
            android:exported="false" />
        <activity
            android:name=".views.Recipe"
            android:exported="false" />
        <activity
            android:name=".views.InputMeal"
            android:exported="false" />
        <activity
            android:name=".views.Home"
            android:exported="false" />
        <activity
            android:name=".views.AccountCreation"
            android:exported="false"
            android:theme="@style/Theme.Material3.Light.NoActionBar" />
        <activity
            android:name=".views.Login"
            android:exported="false"
            android:theme="@style/Theme.Material3.Light.NoActionBar" />
        <activity
            android:name=".views.Welcome"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>